{% extends 'base.html.twig' %}

{% block body %}
    <h1>Place</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ place.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ place.title }}</td>
            </tr>
            <tr>
                <th>Base_desc</th>
                <td>{{ place.basedesc }}</td>
            </tr>
            <tr>
                <th>With_item1</th>
                <td>{{ place.withitem1 }}</td>
            </tr>
            <tr>
                <th>With_item2</th>
                <td>{{ place.withitem2 }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_place_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_place_edit', { 'id': place.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
